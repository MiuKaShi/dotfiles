#!/bin/bash

# Script to choose and open a book in $HOME/books with fzf+zathura
BOOK_DIR="$HOME/papers/pdf"

cd "$BOOK_DIR" || exit
fname=$(fd -e pdf --strip-cwd-prefix | fzfpreview --prompt='papers>' --select-1 --exit-0)

if [[ -z "$fname" ]]; then
    exit 1
fi

zathura "$fname" 
