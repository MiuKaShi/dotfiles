#!/usr/bin/env bash

m3u8="/home/miuka/.config/livetv/sourcelist"

channel=$(cat "$m3u8" | awk -F"," '{print $1}' | fzf)

source=$(cat "$m3u8" |  grep -i "$channel" | awk -F"," '{print $2}')

mpv "$source" --force-seekable=yes

