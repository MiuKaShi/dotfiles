#! /bin/sh

ldp="EV2795" # set display model
hdp1="PHL"
hdp2="HKC"
modeldp=$(sudo ddcutil detect | grep "Mfg" | awk '{print $3}')


case $modeldp in
  *"$hdp1"*) homescreen ;;
  *"$ldp"*) labscreen ;;
esac
