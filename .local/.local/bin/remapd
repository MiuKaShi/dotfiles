#!/bin/bash

# Rerun the remaps script whenever a new input device is added.
# xplugd may be a better solution.

while :; do
    remaps
    grep -qP -m1 '(add.+\/[^:]+\(input\)|[^un]bind.+\/[^:]+\(usb\))' <(udevadm monitor -u -t seat -s input -s usb)
done
