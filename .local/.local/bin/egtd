#!/bin/bash

if ! emacsclient -e "t" >/dev/null 2>&1; then
    emacs --daemon >~/.config/emacs/etc/log/emacs_start.log 2>&1
fi

if [ -n "$1" ]; then
    file=$(readlink -f "$1")
    emacsclient -c -n "$file"
else
    emacsclient -c -n "$HOME/Org/inbox.org"
fi
