#!/bin/bash

# Description: Extract text from subtitle file
file=$(readlink -f "$1")
name=$(basename "$file")
fname="${name%.*}"
ext="${file##*.}"

convert $file -fuzz 10% -transparent white "$fname"_tran."$ext"
mogrify -shave 5x5 "$fname"_tran."$ext" || exit 1
